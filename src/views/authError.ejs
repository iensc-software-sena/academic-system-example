<!DOCTYPE html>
<html lang="es">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="Autenticación invalida">
  <link rel="stylesheet" href="/styles/authError.css" type="text/css">
  <meta name="robots" content="index,follow">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Readex+Pro:wght@700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
  <title>Autenticación invalida</title>
  <script type="application/javascript" src="/js/redirectView.js"></script>
</head>

<body class="body-container">
  <main class="app-container">
    <section class="error-container">
      <div class="icon-container">
        <span class="<%= type === 'user' ? 'fa-solid fa-user-xmark fa-beat' :
                        type === 'password' ? 'fa-solid fa-lock fa-beat' :
                        type === 'no-token' ? 'fa-solid fa-user-shield fa-beat' :
                        type === 'token-exp' ? 'fa-solid fa-right-from-bracket fa-beat' :
                        type === 'token-inv' ? 'fa-solid fa-circle-exclamation fa-beat' : '' %>"></span>
      </div>
      <p class="error-message">
        <%= message %>
      </p>
      <button class="button" data-path="/app/v1/" onclick="redirect(this)">Aceptar</button>
    </section>
  </main>
</body>

</html>
